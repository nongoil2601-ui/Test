name: Cleanup branch after Auto PR and Merge

on:
  workflow_run:
    workflows: ["Auto Create Pull Request and Merge PR"]
    types:
      - completed

jobs:
  cleanup:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Delete branch test
        run: |
          echo "🗑️ Deleting branch test"
          git push origin --delete test || echo "branch already deleted"
